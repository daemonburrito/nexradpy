from setuptools import setup, find_packages

setup(name='nexradpy',
        packages=find_packages(),
        install_requires['Pillow',
            'argparse',
            'matplotlib',
            'numpy'])
